<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <CheckBoxPreference android:title="Use Wifi Only"
        android:defaultValue="false"
        android:summary="This option if selected will allow the application to use Wifi only for this app."
        android:key="applicationUpdates" />
    <CheckBoxPreference android:title="Notification"
        android:defaultValue="true"
        android:summary="This option if selected will allow the application to send Notification to the app."
        android:key="applicationUpdates" />
</PreferenceScreen>